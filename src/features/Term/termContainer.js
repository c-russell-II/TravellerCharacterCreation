import React from "react";
import { Outlet } from "react-router-dom";

const TermContainer = () => {

    return (
        <div>
            <Outlet/>
        </div>
    )
}

export default TermContainer;